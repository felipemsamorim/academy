Aplicativo de avaliação criado com Laravel, utiliza migrations, logo é necessário apenas criar um banco de dados com o mesmo nome do configurado no aplicativo(academy) e as tabelas deverão serem criadas.
Mesmo não sendo requisito explícito da avaliação criei as rotas para PUT, DELETE e POST, para o caso do usuário querer usar o Postman para inserção e etc...